(()=>{const KEY='sanlean-demo-v2';const tbody=document.getElementById('publicStandings');const top=document.getElementById('publicTopFive');if(!tbody||!top)return;let stored=null;try{stored=JSON.parse(localStorage.getItem(KEY)||'null')}catch(e){}const players=stored&&Array.isArray(stored.players)?stored.players:[];if(!players.length)return;const total=p=>(+p[1]||0)*60000+(+p[2]||0)*1000+(+p[3]||0);const fmt=p=>String(+p[1]||0).padStart(2,'0')+':'+String(+p[2]||0).padStart(2,'0')+'.'+String(+p[3]||0).padStart(3,'0');const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const sorted=[...players].sort((a,b)=>total(b)-total(a));tbody.innerHTML=sorted.map((p,i)=>`<tr><td class="rank">${String(i+1).padStart(2,'0')}</td><td>${esc(p[0])}</td><td>${fmt(p)}</td></tr>`).join('');top.innerHTML=sorted.slice(0,5).map((p,i)=>`<div class="top-five-row"><b>${String(i+1).padStart(2,'0')}</b><span>${esc(p[0])}</span><strong>${fmt(p)}</strong></div>`).join('')})();