(()=>{const KEY='sanlean-demo-v2';const tbody=document.getElementById('publicStandings'),top=document.getElementById('publicTopFive'),bestSection=document.getElementById('bestTimesSection'),title=document.getElementById('publicTournamentName'),logo=document.querySelector('#tournamentLogo img'),period=document.getElementById('tournamentPeriod');if(!tbody)return;let stored=null;try{stored=JSON.parse(localStorage.getItem(KEY)||'null')}catch(e){}const tournament=stored&&stored.tournament?stored.tournament:{};const players=stored&&Array.isArray(stored.players)?stored.players:[];const total=p=>(+p[1]||0)*60000+(+p[2]||0)*1000+(+p[3]||0);const played=p=>total(p)>0;const fmt=p=>String(+p[1]||0).padStart(2,'0')+':'+String(+p[2]||0).padStart(2,'0')+'.'+String(+p[3]||0).padStart(3,'0');const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const name=tournament.name||'NOMBRE DEL TORNEO';const words=name.trim().split(/\s+/);title.innerHTML=`<span>${esc(words.shift()||'NOMBRE')}</span>${words.length?' '+esc(words.join(' ')):''}`;if(tournament.logo)logo.src=tournament.logo;if(tournament.period){period.innerHTML=`<option>${esc(tournament.period)}</option>`}const completed=players.filter(played).sort((a,b)=>total(b)-total(a));const waiting=players.filter(p=>!played(p));const sorted=[...completed,...waiting];tbody.innerHTML=sorted.length?sorted.map((p,i)=>`<tr class="${played(p)?'':'pending'}"><td>${i+1}</td><td>${esc(p[0])}</td><td>${played(p)?fmt(p):'—'}</td></tr>`).join(''):'<tr class="pending"><td>1</td><td>—</td><td>—</td></tr>';const requested=Math.max(1,Math.min(5,+tournament.bestTimesCount||5));if(completed.length>=6){bestSection.hidden=false;top.style.setProperty('--best-count',requested);top.innerHTML=completed.slice(0,requested).map((p,i)=>`<div class="best-time-card"><span class="pos">${i+1}</span><span class="time">${fmt(p)}</span><span class="name">${esc(p[0])}</span></div>`).join('')}else{bestSection.hidden=true}})();